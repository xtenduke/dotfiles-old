[user]
	name = Jake Laurie
	email = jake.laurie@weareroam.com
[alias]
	st = status
	co = checkout
	fe = fetch
	cl = clone
	pu = push
	br = branch
	ree = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all

